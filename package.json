{
  "name": "palavraperdida",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "concurrently -n web,api -c blue,green \"npm run dev --prefix web\" \"npm run start:dev --prefix api\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
